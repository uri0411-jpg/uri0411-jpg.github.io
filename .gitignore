# Prototype / demo files — archived, not part of production build
twilight-prototype-v4.html
scripts.zip

# Node / build artifacts
node_modules/
dist/
.cache/

# Environment / secrets
.env
.env.local

# Local IDE / tooling
.claude/

# Android build outputs + signing keystore (NEVER commit secrets)
android/

# Backtest intermediate outputs (regeneratable via scripts/build_learning_dataset.mjs)
scripts/data/
scripts/package-lock.json

# OS
.DS_Store
Thumbs.db
